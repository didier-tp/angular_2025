import { BorderOverDirective } from './border-over.directive';

describe('BorderOverDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderOverDirective();
    expect(directive).toBeTruthy();
  });
});
